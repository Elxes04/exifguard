# ExifGuard Environment Configuration

# Service Ports
WEB_PORT=3000
API_PORT=8080

# API Configuration
PORT=8080
DB_PATH=/app/data/exifguard_public.db
NODE_ENV=production

# Frontend API Endpoint (Set to empty string or /api for Nginx reverse proxy)
VITE_PUBLIC_API_URL=/api
